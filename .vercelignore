# Build output — Vercel builds its own, no need to upload
build/

# Large video files — audio extracted as MP3s in public/sounds/
public/sounds/*.mp4
public/sounds/*.mkv

# Accidental project copies inside project folder
Desktop/
AppData/

# Node modules (Vercel installs its own)
node_modules/
